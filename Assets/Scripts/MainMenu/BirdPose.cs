namespace KiBird.MainMenu
{
    // Les 4 postures détectées par la Kinect (simulées au clavier pour l'instant).
    public enum BirdPose
    {
        Neutral,
        Glide,
        TiltLeft,
        TiltRight,
        FlapUp
    }
}
